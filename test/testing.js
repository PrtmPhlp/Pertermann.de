const assert = require('node:assert');
describe('Array', () => {
  describe('#indexOf()', () => {
    it('should equal 15 when 5 times 3', () => {
      const result = 5 * 3;
      assert.equal(result, 15);
    });
  });
});
