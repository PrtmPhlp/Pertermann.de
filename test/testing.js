var assert = require('assert');
describe('Array', function () {
  describe('#indexOf()', function () {
    it('should equal 15 when 5 times 3', function () {
      var result = 5 * 3;
      assert.equal(result,15);
    });
  });
});
