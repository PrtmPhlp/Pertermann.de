services:
  pertermann.de:
    container_name: webpage
    image: ghcr.io/prtmphlp/pertermann:latest
    build:
      dockerfile: ./Dockerfile
    # ports:
    #  - 3000:3000

networks:
  default:
    external: true
    name: proxy
